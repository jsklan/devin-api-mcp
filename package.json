{
  "name": "@jsklan/devin-api-mcp",
  "version": "0.3.1",
  "description": "MCP server for the Devin API — create, manage, and interact with Devin sessions",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "devin-api-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepublishOnly": "tsc",
    "version": "node -e \"const v=require('./package.json').version; for(const f of ['.claude-plugin/plugin.json','.claude-plugin/marketplace.json']){const j=require('./'+f);j.version?j.version=v:j.metadata&&(j.metadata.version=v);require('fs').writeFileSync(f,JSON.stringify(j,null,2)+'\\n')}\" && git add .claude-plugin/plugin.json .claude-plugin/marketplace.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.3.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jsklan/devin-api-mcp.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}
